spring:
  application:
    name: tx-service
  jpa:
    open-in-view: false
server:
  port: 8082

security:
  jwt:
    issuer: cloud-fintech-auth
    audience: cloud-fintech-tx
    secret: "CHANGE-ME-IN-DEV-ONLY-AT-LEAST-32-CHARS-LONG"


---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fintech}
    username: ${DB_USER:fintech}
    password: ${DB_PASSWORD:fintech}

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fintech}
    username: ${DB_USER:fintech}
    password: ${DB_PASSWORD:fintech}

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fintech}
    username: ${DB_USER:fintech}
    password: ${DB_PASSWORD:fintech}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration